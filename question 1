import random
import numpy as np

def random_array(row,column):
    a = np.random.randint(0, 100, size=(row,column))
    return a
    
x = input("Enter  row_size: ")
y = input("Enter column_size: ")
z = random_array(x,y)
print(z)
